cfgs:
  default:
    branches: ['master']

  release-branches:
    branches: ['rel-.*', 'hotfix-.*']
    inherit:
      test-infra:
        jobs:
          release:
            traits:
              version:
                preprocess: 'finalize'
              release:
                nextversion: 'bump_patch'
